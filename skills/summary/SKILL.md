---
name: summary
description: |
  对已就绪的内容做类型识别、提炼总结并写入本地知识库。本 skill 不定义抓取流程，
  内容须由调用方或用户预先准备好（粘贴、其他工具抓取、上游 skill 传入均可）。触发条件：
  - 用户提供链接或内容并要求总结、学习或提炼
  - 用户说"帮我总结这篇文章"、"提炼一下这个链接"
  - 用户说"保存到知识库"、"记录一下这篇"
---

# summary：内容提炼与知识库存档

对已就绪的内容做类型识别、按类型套用模板提炼核心观点，并写入本地知识库。

**本 skill 不负责内容抓取**。内容如何获取（用户粘贴、浏览器抓取、上游 skill 传入等）由调用方决定；
进入本 skill 时应已具备可直接提炼的正文。

调用方可覆盖默认信息卡字段：
- 提供字段清单（顺序、值、需省略的字段）时按调用方约定执行；未指定字段沿用默认规则
- 可指定 `类型` 为非 4 类的特殊值（如 `学习计划`、`调研报告`），此时跳过类型判断步骤

---

## 执行流程

### Step 0：信息收集

**先解析输入 → 再自补 → 确认后动手**

#### 解析清单

| 信息项 | 若缺失，如何处理 |
|-------|--------------|
| 正文内容 | **必须由用户/调用方提供**，无内容则无法继续 |
| 原始来源（URL / 出处） | 可空，作为信息卡 `来源` 字段；空缺时省略该字段 |
| 阅读重点/关注角度 | 默认"全面提炼"，在摘要中标注 |
| 保存路径 | 默认 `~/knowledge/`，可在确认时修改 |

#### 理解摘要模板

解析完成后展示摘要，**紧跟 `AskUserQuestion` 确认**：

> 我的理解：
> - 来源：**[URL / 出处 / 无]**
> - 关注角度：**[全面提炼 / 用户指定角度]**
> - 保存路径：**`~/knowledge/YYYY-MM-DD-{标题}.md`**

```
AskUserQuestion({
  questions: [{
    header: "确认信息",
    question: "以上理解是否正确？",
    multiSelect: false,
    options: [
      { label: "确认，开始提炼", description: "信息无误，立即开始" },
      { label: "需要调整", description: "请在 Other 文本框中说明" }
    ]
  }]
})
```

<HARD-GATE>
未展示摘要并收到用户确认前，不得开始提炼与写入。
（注：本 skill 由上游 skill 内联调用且已携带明确意图时，可跳过此确认）
</HARD-GATE>

---

### Step 1：识别类型并提炼

#### 类型判断（按优先级匹配）

| 类型 | 判断信号 |
|------|---------|
| **技术教程** | 有步骤编号 / 代码块；标题含「如何 / 使用 / 入门 / 指南 / 教程」；以操作为主 |
| **原理架构** | 有设计讨论 / 系统对比 / 深度分析；标题含「原理 / 架构 / 设计 / 深入 / 分析」 |
| **行业资讯** | 事件驱动；含时间 / 数据 / 发布公告；主体是外部事件而非技术本身 |
| **思想观点** | 论点驱动；个人见解 / 方法论 / 反思；标题含「为什么 / 如何思考 / 我认为」 |

无法判断时默认「**原理架构**」。

#### 模板 A：技术教程

```markdown
## 🎯 功能定位

> 一句话说清这个工具/技术/框架做什么、解决什么问题。

## ⚡ 快速上手

核心操作步骤（3-5 步），关键代码片段保留：

1. **{步骤 1}** — {做什么 + 关键命令}
2. **{步骤 2}** — ...

## 🔧 核心 API / 命令

| API/命令 | 作用 | 关键参数 |
|---|---|---|
| ... | ... | ... |

## ⚠️ 常见坑

- **{坑 1}** — 现象 + 正确做法
- **{坑 2}** — ...

## 💡 适用场景

什么情况下应该用？哪些情况不适合？
```

#### 模板 B：原理架构

```markdown
## 🎯 核心设计理念

> 为什么要这样设计？解决了什么根本问题？

## 🏗️ 关键架构决策

3-5 个核心设计选择，每个写「是什么 + 为什么这样选」：

- **{决策 1}** — {内容}；选择理由：{...}
- **{决策 2}** — ...

## ⚖️ 技术权衡

**优势**

- ...

**劣势 / 局限**

- ...

## 🔄 与现有方案对比

| 维度 | 本方案 | 其他方案 |
|---|---|---|
| ... | ... | ... |

## 💡 启发与应用

这个设计理念能在自己的哪些场景中借鉴？
```

#### 模板 C：行业资讯

```markdown
## 📰 核心事件

> 2-3 句话说清楚发生了什么。

## 📊 关键数据 / 时间线

- **{YYYY-MM-DD}** — {事件节点 1}
- **{YYYY-MM-DD}** — {事件节点 2}

## 🌊 影响分析

**对行业**

- ...

**对自己 / 团队**

- ...

## 🤔 我的判断

这件事重要吗？为什么？倾向于怎么看？

## 🔮 后续值得关注

接下来应该关注什么信号或进展？
```

#### 模板 D：思想观点

```markdown
## 💭 核心论点

> 作者主要主张什么？用自己的话概括。

## 📚 支撑论据

作者用哪些论据支撑？质量如何？

- **{论据 1}** — {内容}
- **{论据 2}** — ...

## ✨ 启发与应用

读完能用在哪里？改变了什么认知？

## 🔍 反驳与局限

观点的局限性或反例？

## 💬 一句话总结

> 推荐给同事时，怎么用一句话讲清它的价值？
```

---

### Step 2：写入本地知识库

**文件路径**：`~/knowledge/YYYY-MM-DD-{slugified-title}.md`

- 标题 slug 规则：取文章标题，空格替换为 `-`，去除特殊字符，全小写（中文标题保留原文）
- 由其他 skill 调用时建议：`~/knowledge/YYYY-MM-DD-{来源skill}-{主题}.md`（如 `learning-Rust`）

**文件格式**（**禁止使用 YAML frontmatter**，统一用信息卡）：

```markdown
# {文章标题}

> **来源** {原始链接 / 出处}  
> **日期** {YYYY-MM-DD}  
> **类型** {技术教程 / 原理架构 / 行业资讯 / 思想观点}  
> **标签** {tag1} · {tag2} · {tag3}  
> **关键概念** {concept1} · {concept2} · {concept3}

---

{提炼内容正文}
```

**信息卡书写规则**：
- 用单层 `>` 引用块包裹，每行末尾保留**两个空格**实现换行（Markdown 强制换行语法）
- 字段名加粗，与值之间用一个空格
- `标签`、`关键概念` 多个值之间用 ` · `（中文间隔号，前后各空格）连接，不要用方括号或逗号
- `标签` 反映文章所属领域（最多 4 个）；`关键概念` 提取文章核心术语（3-5 个，便于后续按概念检索）
- 字段顺序固定：来源 → 日期 → 类型 → 标签 → 关键概念；可省略不适用的字段，但保持此顺序
- 信息卡下方留一空行，再写 `---` 分隔线，再留空行后开始正文

写入完成后告知用户文件路径：

> ✓ 已保存到 `{文件路径}`

---

## 质量标准

- **来源有据**：提炼内容来自给定正文，不补充原文没有的信息
- **结构精简**：每个要点一句话，不做大段复述
- **类型契合**：选定的模板章节须与文章内容匹配；若多数章节空洞，说明类型选错，重新判断
- **标签准确**：`标签` 反映领域（≤4 个），`关键概念` 反映文章独有术语（3-5 个），二者不要重复
- **emoji 仅章节标题用**：正文不要散布 emoji，保持阅读节奏
- **内容缺失透明**：若调用方/用户未提供可用正文，直接告知并请求补充，不基于猜测生成内容
